batch_size: 32
seq_len: 1024
max_steps: 10000
warmup_steps: 500
learning_rate: 3.0e-4
weight_decay: 0.1
grad_clip: 1.0
precision: bfloat16
gradient_checkpointing: true
log_every: 50
eval_every: 500
save_every: 1000
checkpoint_dir: checkpoints
